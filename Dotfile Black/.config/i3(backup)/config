# This file has been auto-generated by i3-config-wizard(1).
# It will not be overwritten, so edit it as you like.
#
# Should you change your keyboard layout some time, delete
# this file and re-run i3-config-wizard(1).
#

# i3 config file (v4)
#
# Please see http://i3wm.org/docs/userguide.html for a complete reference!

set $mod Mod4

# Font for window titles. Will also be used by the bar unless a different font
# is used in the bar {} block below.
font pango: System San Francisco Display 10

# This font is widely installed, provides lots of unicode glyphs, right-to-left
# text rendering and scalability on retina/hidpi displays (thanks to pango).let g:airline_theme='powelet g:airline_theme='powerlineish'

# Before i3 v4.8, we used to recommend this one as the default:
# font -misc-fixed-medium-r-normal--13-120-75-75-C-70-iso10646-1
# The font above is very space-efficient, that is, it looks good, sharp and
# clear in small sizes. However, its unicode glyph coverage is limited, the old
# X core fonts rendering does not support right-to-left and this being a bitmap
# font, it doesn’t scale on retina/hidpi displays.

# Use Mouse+$mod to drag floating windows to their wanted position
floating_modifier $mod

# enable or disable focus on mouse hover (yes | no)
focus_follows_mouse no

# start a terminal
#bindsym $mod+Return exec xfce4-terminal
bindsym $mod+Return exec --no-startup-id terminator

# kill focused window
bindsym $mod+Shift+q kill

# start rofi (a program launcher)
#bindsym $mod+d exec --no-startup-id i3-dmenu-desktop --dmenu='rofi -dmenu -lines 8 -width 500 -i -sort'
#bindsym $mod+d exec --no-startup-id j4-dmenu-desktop --dmenu="dmenu -fn 'xos4 Terminess Powerline-9' -nb '#268BD2' -nf '#FFFFFF' -sb '#FFFFFF' -sf '#002B36' -i -h '21'"
bindsym $mod+d exec rofi -lines 12 -padding 18 -width 60 -location 0 -show drun -sidebar-mode -columns 3 -font 'Noto Sans 8'

# change focus
bindsym $mod+Tab focus left
bindsym $mod+j focus down
bindsym $mod+k focus up
bindsym $mod+Shift+Tab focus right

# alternatively, you can use the cursor keys:
bindsym $mod+Left focus left
bindsym $mod+Down focus down
bindsym $mod+Up focus up
bindsym $mod+Right focus right

# move focused window
bindsym $mod+Shift+h move left
bindsym $mod+Shift+j move down
bindsym $mod+Shift+k move up
bindsym $mod+Shift+l move right

# alternatively, you can use the cursor keys:semicolon
bindsym $mod+Shift+Left move left
bindsym $mod+Shift+Down move down
bindsym $mod+Shift+Up move up
bindsym $mod+Shift+Right move right

# split in horizontal orientation
bindsym $mod+b split h

# split in vertical orientation
bindsym $mod+v split v

# enter fullscreen mode for the focused container
bindsym $mod+f fullscreen toggle

# change container layout (stacked, tabbed, toggle split)
bindsym $mod+s layout stacking
bindsym $mod+w layout tabbed
bindsym $mod+e layout toggle split

# orientation for new workspaces
# New workspaces get a reasonable default orientation: Wide-screen monitors
# (anything wider than high) get horizontal orientation, rotated monitors
# (anything higher than wide) get vertical orientation.
# syntax: default_orientation horizontal|vertical|auto
default_orientation auto

# layout mode for new containers
# this option determines in which mode new containers on workspace level will start
# syntax: workspace_layout default|stacking|tabbed
workspace_layout tabbed


# toggle tiling / floating
bindsym $mod+Shift+space floating toggle border normal 1

# change focus between tiling / floating windows
bindsym $mod+space focus mode_toggle

# focus the parent container
# bindsym $mod+a focus parent

# focus the child container
# bindsym $mod+Shift+a focus child

# Make the currently focused window a scratchpad
# bindsym $mod+Shift+minus move scratchpad

# Show the first scratchpad window
# bindsym $mod+minus scratchpad show

set $ws1 "1 "
set $ws2 "2 "
set $ws3 "3 "
set $ws4 "4 "
set $ws5 "5 "
set $ws6 "6 "
set $ws7 "7 "
set $ws8 "8 "
set $ws9 "9 "
set $ws10 "10 "

# switch to workspace
bindsym $mod+1 workspace $ws1
bindsym $mod+2 workspace $ws2
bindsym $mod+3 workspace $ws3
bindsym $mod+4 workspace $ws4
bindsym $mod+5 workspace $ws5
bindsym $mod+6 workspace $ws6
bindsym $mod+7 workspace $ws7
bindsym $mod+8 workspace $ws8
bindsym $mod+9 workspace $ws9
bindsym $mod+0 workspace $ws10

# move focused container to workspace
bindsym $mod+Shift+1 move container to workspace $ws1
bindsym $mod+Shift+2 move container to workspace $ws2
bindsym $mod+Shift+3 move container to workspace $wS3
bindsym $mod+Shift+4 move container to workspace $ws4
bindsym $mod+Shift+5 move container to workspace $ws5
bindsym $mod+Shift+6 move container to workspace $ws6
bindsym $mod+Shift+7 move container to workspace $ws7
bindsym $mod+Shift+8 move container to workspace $ws8
bindsym $mod+Shift+9 move container to workspace $ws9
bindsym $mod+Shift+0 move container to workspace $ws10

# Assign Startup Application to Specific Workspace
#assign [class="Spotify"] $ws10


# Enable Floating Mode for Specific Programs
#for_window [class="Gkamus"] floating enable border normal 1
#for_window [class="KBBI-Qt"] floating enable border normal 1
#for_window [class="Gnome-power-statistics"] floating enable border normal 1



# Disable Floating Mode for Specific Programs
#for_window [class="pavucontrol-qt"] floating disable

# reload the configuration file
bindsym $mod+Shift+c reload

# restart i3 inplace (preserves your layout/session, can be used to upgrade i3)
bindsym $mod+Shift+r restart

# resize window (you can also use the mouse for that)
bindsym $mod+r mode "resize"

mode "resize" {
  # These bindings trigger as soon as you enter the resize mode

  # Pressing left will shrink the window’s width.
  # Pressing right will grow the window’s width.
  # Pressing up will shrink the window’s height.
  # Pressing down will grow the window’s height.
  bindsym h resize shrink width 2 px or 2 ppt
  bindsym j resize grow height 2 px or 2 ppt
  bindsym k resize shrink height 2 px or 2 ppt
  bindsym l resize grow width 2 px or 2 ppt

  # same bindings, but for the arrow keys
  bindsym Left resize shrink width 2 px or 2 ppt
  bindsym Down resize grow height 2 px or 2 ppt
  bindsym Up resize shrink height 2 px or 2 ppt
  bindsym Right resize grow width 2 px or 2 ppt

  # back to normal: Enter or Escape
  bindsym Return mode "default"
  bindsym Escape mode "default"
}

# Start i3bar to display a workspace bar (plus the system information bumblebee-status
# finds out, if available)

set $bg-color 	      		#002b36
set $border-color		#002b36
set $inactive-bg-color   	#002b36
set $text-color          	#74a8b8
set $inactive-text-color        #676E7D
set $urgent-bg-color            #073642
set $urgent-text-color		#268BD2

# window colors
#                       border              background         text                 indicator
client.focused          $border-color	    $border-color      $text-color          #ffaf00
client.unfocused        $inactive-bg-color  $inactive-bg-color $inactive-text-color #ffaf00
client.focused_inactive $inactive-bg-color  $inactive-bg-color $inactive-text-color #ffaf00
client.urgent           $urgent-bg-color    $urgent-bg-color   $urgent-text-color   #ffaf00

# Hiding edge border indicator
hide_edge_borders both

# Window Border Settings
# format : new_window normal|none|pixel
new_window pixel 1
# format : new_float normal|none|pixel
new_float normal 1

#bindsym $mod+t border normal 1
#bindsym $mod+Shift+t border normal 0
#bindsym $mod+y border none
#bindsym $mod+Shift+y border pixel 1

# Enable Specific Window Border
# for_window [class="Wine"] border normal 1

# i3bar Toggle between dock mode and hide mode
#bindsym $mod+F11 bar mode toggle

# bar {
#     status_command echo '{"version":1}[[],'; xtitle -s -f '[{"full_text": "%s"}],'
# }

# bar {
#   id bar-1
#   tray_output primary
#   position top
#   height 20 # this feature of i3-gaps
#   # Bumblebee-Status on GitHub https://github.com/tobi-wan-kenobi/bumblebee-status
#   status_command ~/.config/bumblebee-status/bumblebee-status -m title brightness pasink pasource memory sensors battery1 battery0 date profile -p date.format="0%w%y%m%d%H%M" -t gruvbox-powerline-bandit-blue-nosymbol
#   #status_command i3blocks
#   font pango:xos4 Terminess Powerline 9
#   bindsym button1 nop
#
#   colors {
#     background  #268BD2
#     separator   #E4E4E4
#     statusline  #E4E4E4
#
#     #		              	border                background          text
#     focused_workspace  		$text-color  	      #268BD2             $text-color
#     active_workspace            #6DAFDE               #268BD2             $text-color
#     inactive_workspace 		#6DAFDE		      #268BD2		  $text-color
#     urgent_workspace   		$urgent-bg-color      $urgent-bg-color    $urgent-text-color
#     #binding_mode       	#2f343a 	      #900000         	  #E4E4E4
#   }
#
# }


################################################################################
####################### DITAMBAHKAN OLEH BANDITHIJO ############################
################################################################################

# For adding space and [] character on window title
# for_window [class=".*"] title_format "[] %title -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------"
# for_window [class=".*"] title_format " [] %title "

# Background Color
#exec_always --no-startup-id xsetroot -solid "#002B36"

# Background Wallpaper
# exec_always --no-startup-id feh --bg-fill -Z /home/bandithijo/pix/Wallpapers/archWallpaper/Arch-Wallpaper-11.png

# i3lock lockscreen
# bindsym $mod+Shift+x exec --no-startup-id i3lock -u --beep --color 002B36 -t -i /home/bandithijo/pix/Wallpapers/archWallpaper/Arch-Wallpaper-11L.png

# exec_always killall xautolock ; xautolock -time 30 -locker "i3lock -u --beep --color 002B36 -i /home/bandithijo/pix/Wallpapers/archWallpaper Arch-Wallpaper-11L.png" && systemctl suspend

# set $Locker i3lock -u --beep --color 002B36 -i /home/bandithijo/pix/Wallpaper/archWallpaper/Arch-Wallpaper-11L.png && systemctl suspend

# Restart, Suspend, Shutdown, Logout Dialog
# bindsym $mod+End exec --no-startup-id ~/.local/bin/rofi-power "i3-msg exit"

# Screenshot
#bindsym $mod+Print exec scrot 'Screenshot\ from\ %Y-%m-%d %H-%M-%S.png' -e 'mv *.png ~/pix/ScreenShot/'
#bindsym $mod+Alt+Print exec scrot -s 'Screenshot\ from\ %Y-%m-%d %H-%M-%S.png' -e 'mv *.png ~/pix/ScreenShot/'
#bindsym $mod+Ctrl+Print exec scrot -u 'Screenshot\ from\ %Y-%m-%d %H-%M-%S.png' -e 'mv *.png ~/pix/ScreenShot/'



# Screen Brightness controls
bindsym XF86MonBrightnessUp exec --no-startup-id xbacklight -inc 2
bindsym XF86MonBrightnessDown exec --no-startup-id xbacklight -dec 2

# Network Manager Applet
#exec killall nm-applet ; nm-applet

# XFCE4-power-manager
# exec killall xfce4-power-manager ; xfce4-power-manager

# Firewalld
#exec killall firewall-applet ; firewall-applet

# KeePassXC
#exec killall keepassxc ; keepassxc

# clipmenu
#bindsym $mod+shift+p exec --no-startup-id clipmenu -i -fn hack:size=10 -nb '#002B36' -nf '#839496' -sb '#E4E4E4' -sf '#002B36'

# Blueman Applet
# exec_always killall blueman-applet ; blueman-applet

# Caps Lock Indicator
#bindsym Caps_Lock exec notify-send 'CAPS LOCK'
#bindsym --release Caps_Lock exec pkill -SIGRTMIN+11 i3blocks

# Disable TouchPad
# exec --no-startup-id xinput --disable "SynPS/2 Synaptics TouchPad"

# Enable Tap to Click
#exec --no-startup-id xinput --set-prop "SynPS/2 Synaptics TouchPad" "libinput Tapping Enabled" 1

# Disable F1 Help Button
#bindsym F1 exec --no-startup-id echo "help" > /dev/null

# Arandr
# bindsym $mod+F7 exec --no-startup-id arandr

# Use pactl to adjust volume in PulseAudio.
#set $refresh_i3status killall -SIGUSR1 i3status
bindsym XF86AudioRaiseVolume exec --no-startup-id pactl set-sink-volume @DEFAULT_SINK@ +5% && $refresh_i3status
bindsym XF86AudioLowerVolume exec --no-startup-id pactl set-sink-volume @DEFAULT_SINK@ -5% && $refresh_i3status
bindsym XF86AudioMute exec --no-startup-id pactl set-sink-mute @DEFAULT_SINK@ toggle && $refresh_i3status
bindsym XF86AudioMicMute exec --no-startup-id pactl set-source-mute @DEFAULT_SOURCE@ toggle && $refresh_i3status

# redshift
# for get the coordinates of places with http://www.geonames.org/
# Jakarta
#exec --no-startup-id redshift -l -6.21462:106.84513 -r
# Balikpapan
#exec --no-startup-id redshift -l -1.26753:116.82887 -r

# Polkit-GNOME
# exec --no-startup-id /usr/lib/polkit-gnome/polkit-gnome-authentication-agent-1

# Polkit-lxsession
# exec --no-startup-id /usr/bin/lxsession

# Disable Middle Mouse Paste
# exec --no-startup-id xinput set-button-map "TPPS/2 IBM TrackPoint" 1 0 3

# i3-msg border none
exec --no-startup-id i3-msg border none

# Start Conky
# exec killall conky ; conky -c ~/.config/conky/conkyrc

# Enable dnscrypt-proxy
#exec --no-startup-id sudo systemctl start dnscrypt-proxy && sudo sed -i s/192.168.1.1/127.0.0.1/g /etc/resolv.conf
exec --no-startup-id $HOME/app/dnscrypt/linux-x86_64/dnscrypt-proxy start

# Enable Compton for compositor
exec_always killall compton ; compton -b --respect-prop-shadow --config ~/.config/compton/compton.conf

# Enable Dunst Notifications
# exec_always killall dunst ; dunst -config '/home/bandithijo/.config/dunst/dunstrc'

# Enable Polybar
exec_always /home/mufrad/.config/polybar/launch.sh

# Walpaper
exec_always --no-startup-id feh --bg-scale /home/mufrad/Pictures/arch-dark.png

# You can also use any non-zero value if you'd like to have a border
#for_window [class=".*"] border pixel 0

# Configure border style <normal|1pixel|pixel xx|none|pixel>
new_window pixel 1
new_float normal

# change borders
bindsym $mod+p border none
bindsym $mod+o border pixel 1
bindsym $mod+i border normal

# Network Manager Dmenu
bindsym $mod+F8 exec --no-startup-id networkmanager_dmenu

